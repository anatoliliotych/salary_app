.well{ style: "height: 76px;" }
  - username = session[:current_user].split('@').first.split('.').map!{ |e| e.capitalize! }.join(' ')
  %form{ action: "/logout", method: "GET", enctype:'multipart/form-data' }
    .container
      .row
        .col-md-3
          %p
            Вы вошли под пользователем #{ username }!
        .col-md-3
          %button{ type: "submit", class: "btn btn-primary btn-xs", style: "width: 76px;"}='Выйти'
